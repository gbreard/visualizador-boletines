# PostgreSQL connection (Neon)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Si no se configura DATABASE_URL, el dashboard usa archivos locales (data/processed/, data/optimized/)
